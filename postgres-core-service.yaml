apiVersion: v1
kind: Service
metadata:
  name: postgres-core
  labels:
    app: postgres-core
spec:
  selector:
    app: postgres-core
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
